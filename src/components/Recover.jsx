import React from 'react';
import PropTypes from 'prop-types';

const Recover = props => {
    return (
        <div>
RECOVER
        </div>
    );
};

Recover.propTypes = {
    
};

export default Recover;